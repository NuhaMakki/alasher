<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>القراءات العشر الكبرى</title>
    <link rel="stylesheet" href="./css/Styles.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Reem+Kufi:wght@400;500;600;700&display=swap" rel="stylesheet">



    <script type="text/javascript"> 
    
        function selects(){  
            var ele=document.getElementsByName('RawyID');  
            for(var i=0; i<ele.length; i++){  
                if(ele[i].type=='checkbox')  
                    ele[i].checked=true;  
            }  
        }  
        function deSelect(){  
            var ele=document.getElementsByName('RawyID');  
            for(var i=0; i<ele.length; i++){  
                if(ele[i].type=='checkbox')  
                    ele[i].checked=false;  
                  
            }  
        }  
        


    </script>  


</head>



<body>
  
    <div id="header">
        <header>
            <h1>العشر الكبرى</h1>
        </header>
    </div>


<form action="/searchingType" method="post">

    <div class="button_container">
            <div class="buttonA">
                <button  class="buttonB buttonC uncolored" name="searchingType" value="true" >
                    البحث بالآيات   
                </button>
            </div>
    
            <div class="buttonA">
                <button  class="buttonB buttonD colored"  name="searchingType" value="false" >
                    البحث بالخلافات
                </button>
            </div>   
        
    </div>

</form>




    <br>
    
    <% if(searchType === 'true'){ %>
        <%- include('Ayah.ejs') %>
    <% } else{ %>  
        <%- include('Khelaf.ejs') %>
    <% } %>
   



    <div id="footer">
        <footer>

        </footer>
    </div>



</body>
</html>



<script>

    //  check if at least one checkbox is checked
    function atLeastOneCheckboxChecked(checkboxes) {
    return Array.from(checkboxes).some(
        checkbox => checkbox.checked,
    );
    }

    const form = document.getElementById('Forms');
    const checkboxes = document.querySelectorAll('input[type="checkbox"]', );

    // check if at least one checkbox is checked when
    // submitting the form
    form.addEventListener('submit', event => {


    if (!atLeastOneCheckboxChecked(checkboxes)) {
        event.preventDefault();
        alert("يرجى تحديد الرواة");
        return;
    }


    });

</script>